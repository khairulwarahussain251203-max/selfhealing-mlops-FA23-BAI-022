apiVersion: v1
kind: Service
metadata:
  name: sentiment-api-service
spec:
  type: NodePort
  selector:
    app: sentiment-api
    slot: blue
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 32500
